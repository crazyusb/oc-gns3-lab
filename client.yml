---
 - hosts: localhost
   become: yes
   tasks:

# Maybe need pip for GNS3 client if no pkg available

    - name: Add ppa
      apt_repository:
         repo: ppa:gns3/ppa
   
    - name: Install gns3 packages
      apt:
       update_cache: yes
       pkg:
       - gns3-gui
